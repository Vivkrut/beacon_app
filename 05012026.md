# 05 Jan 2026 – Dev Log

## What changed today
- Updated SMS flow to auto-send via Telephony, deduplicate recipients, and fall back to opening the SMS composer for any blocked numbers. (lib/features/communication/services/sms_service.dart)
- Home SOS flow now reports partial send results (auto-sent vs composer fallback) and treats SOS as sent only if at least one auto SMS succeeded. (lib/features/home/presentation/screens/home_screen.dart)
- Retested on device: permissions flow stable; BlackBox logs SOS with evidence path; SMS delivery still device-dependent (composer fallback should appear if auto-send is blocked).

## Status
- Auto-call works; evidence recording works; SOS rate-limit and permission gating in place.
- Auto-SMS depends on device/OEM allowance; fallback composer used when blocked.

## Next steps (tomorrow)
- Re-run on device, confirm SMS delivery or composer appearance; adjust sent/delivered callbacks if needed.
- Clean up DB migration warnings (duplicate columns) by gating ALTER statements.
- Verify BlackBox detail screen opens evidence files reliably.
